# Web Audit Tool

A simple website security configuration audit tool.

## Install

                    Termux🔥💯

```
pkg update
pkg install git python -y
git clone https://github.com/BlackTerminal-2/web-audit.git
cd web-audit
pip install -r requirements.txt
python audit.py
```